(library
  (name common)
  (modules (:standard \ tests))
  (libraries compiler-libs.common containers containers-data iter fmt))

(test
  (name tests)
  (modules tests)
  (libraries common)
  (flags (:standard -open Common)))
